# ── PyInstaller 产物 ────────────────────────────────────────
# 16MB 的 exe 不进版本库，发版时作为 Release 附件上传
build/
dist/
*.spec

# ── Python ─────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── 调试时生成的预览图 ───────────────────────────────────────
# 用 `python _preview_ui.py && python _shot.py` 可随时重建
_preview.html
_preview.png

# ── 从站点下载的测试样本 ─────────────────────────────────────
# 里面是真实课程资料（试卷 PDF），不要入库
_samples/

# ── 本地诊断输出 ────────────────────────────────────────────
exe_icon_extracted.png
